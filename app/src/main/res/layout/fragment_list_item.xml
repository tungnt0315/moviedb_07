<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    >

    <data>
        <variable
            name="viewModel"
            type="com.example.tung.moviedb_07.screen.list_item.ListItemViewModel"
            />
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        >

        <android.support.v7.widget.RecyclerView
            android:id="@+id/recyclerLinear"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:OnScrollListener="@{viewModel.onScrollListener}"
            app:RecyclerGetPosition="@{(recyclerGrid)}"
            app:ScrollToPosition="@{viewModel.scrollToFirst}"
            app:Visibility="@{viewModel.linearLayoutVisibility}"
            app:adapter="@{viewModel.adapterLinear}"
            app:layoutManager="LinearLayoutManager"
            />

        <android.support.v7.widget.RecyclerView
            android:id="@+id/recyclerGrid"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:OnScrollListener="@{viewModel.onScrollListener}"
            app:RecyclerGetPosition="@{(recyclerLinear)}"
            app:ScrollToPosition="@{viewModel.scrollToFirst}"
            app:Visibility="@{!viewModel.linearLayoutVisibility}"
            app:adapter="@{viewModel.adapterGrid}"
            app:layoutManager="GridLayoutManager"
            app:spanCount="3"
            />

        <ImageView
            android:id="@+id/imageView"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:layout_alignParentBottom="true"
            android:layout_alignParentEnd="true"
            android:layout_alignParentRight="true"
            android:layout_marginBottom="11dp"
            android:layout_marginEnd="5dp"
            android:layout_marginRight="5dp"
            android:onClick="@{viewModel::onImageUpClicked}"
            android:src="@drawable/icon_arrow_up"
            android:translationZ="5dp"
            app:Visibility="@{viewModel.imageUpVisibility}"
            tools:ignore="ContentDescription"
            tools:targetApi="lollipop"
            />

    </RelativeLayout>

</layout>
